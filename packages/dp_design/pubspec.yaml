name: dp_design
description: DevPath AI 디자인 시스템(Material 3). 도메인 모름. 토큰 SSoT=DESIGN.md.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.12.1
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  material_symbols_icons: ^4.2815.0   # 하한 가이드 — 설치 결과로 확정
  markdown_widget: ^2.3.2+8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
