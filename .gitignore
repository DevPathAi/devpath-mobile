# 공통
node_modules/
dist/
build/
*.local
.DS_Store

# 로그
*.log
npm-debug.log*

# 에디터
.idea/
.vscode/*
!.vscode/extensions.json
*.iml

# Flutter (mobile)
apps/mobile/.dart_tool/
apps/mobile/.flutter-plugins
apps/mobile/.flutter-plugins-dependencies
apps/mobile/build/

# Dart pub workspace 루트 산출물 (E2 — melos bootstrap이 루트에 생성)
/.dart_tool/

# 환경 변수 — 절대 커밋 금지
.env
.env.*
!.env.example
!.env.local.example

# 브레인스토밍 비주얼 컴패니언 (로컬 전용)
.superpowers/
.omc/
.gstack/

# 브라우저 UX 러너 리포트(CI 아티팩트로만 보존)
evidence/browser-ux/

# 성능 측정 리포트(CI 아티팩트로만 보존)
evidence/perf/

# design-sync (claude.ai/design) local artifacts
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
